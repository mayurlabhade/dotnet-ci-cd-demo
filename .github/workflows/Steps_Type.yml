name: Learning_GitHub_Actions
on:
  workflow_dispatch:
jobs:
  Scan:
    runs-on: ubuntu-latest
    steps:
     - run: echo "This is Scan Job"
     - run: print("This is Scan Job From Python")
       shell: python
     - run: actions/setupdotnet@v4
       with:
         dotnet_version: 10.*background:  
